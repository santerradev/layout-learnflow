import { Dashboard } from './Dashboard';

/**
 * Main Index page - redirects to Dashboard
 * This serves as the entry point for the EduPlatform application
 */
const Index = () => {
  return <Dashboard />;
};

export default Index;
